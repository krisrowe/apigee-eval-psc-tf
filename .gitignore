# OS files (Ignored to keep the repo clean of machine-specific artifacts)
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# --- Logs & Zips ---
*.log
**/*.log
# Ignore all zips to avoid committing Apigee proxy bundles or large binary archives
*.zip
**/*.zip

# --- Terraform Exclusions ---

# Local provider cache and binary plugins (Large and machine-specific)
.terraform/

# State files (Ignored because we use a centralized state directory managed by the CLI)
*.tfstate
*.tfstate.*
.terraform.tfstate.lock.info

# --- Terraform Tracking (EXPLICITLY INCLUDED) ---

# Track these to ensure a consistent, reproducible project state across environments
!.terraform.lock.hcl
!apigee.tfvars

# --- Python ---
.venv/
__pycache__/
*.pyc
*.egg-info/

# --- Environment & Misc ---
.tmp/
temp/
.gemini/
*.bak
*.tmp
